import 'package:flutter/material.dart';

class AppRadius {
  AppRadius._();
  static const badge  = BorderRadius.all(Radius.circular(4));
  static const button = BorderRadius.all(Radius.circular(6));
  static const card   = BorderRadius.all(Radius.circular(8));
  static const avatar = BorderRadius.all(Radius.circular(9999));
}